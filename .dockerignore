# Что не отправляется в сборку образа.
#
# Главное здесь — node_modules. Без этой строки папки с машины разработчика
# копируются в образ поверх того, что поставил pnpm, и внутрь попадают чужие
# пакеты вместе со сломанными ссылками.
node_modules
**/node_modules

dist
build
**/dist
**/out
**/release

.git
.env
.env.*
backups
*.log

# Проверочные скрипты и снимки экрана: к образу отношения не имеют.
*.mjs
Dockerfile.*
docker-compose.check.yml
