-- Ссылка на оплату хранится с платежом: повторный показ должен вернуть тот же
-- QR, иначе кассир видит пустоту вместо кода при втором клике.
ALTER TABLE "PaymentIntent" ADD COLUMN "checkoutUrl" TEXT;
