-- Отметка о том, до какого момента сверены отчёты агента об автономной работе.
-- Без неё повторный отчёт за тот же период возвращает деньги гостю дважды.
ALTER TABLE "Session" ADD COLUMN "offlineReconciledAt" TIMESTAMP(3);
