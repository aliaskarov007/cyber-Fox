-- Сервер больше не начисляет за время, которое агент не подтвердил, поэтому
-- переплаты не возникает и сверять период возвратов нечем.
ALTER TABLE "Session" DROP COLUMN IF EXISTS "offlineReconciledAt";

-- Сессия машины, чей агент давно не выходил на связь, закрывается: иначе зал
-- показывает занятым место, за которым никто не играет.
ALTER TYPE "SegmentEndReason" ADD VALUE 'AGENT_LOST';
